# Compatibility export for pip-only environments.
# Use `uv sync --locked` when possible; uv.lock is the authoritative,
# fully resolved dependency set for this project.
-r requirements-colab-resolved.txt
